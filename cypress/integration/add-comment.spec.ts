describe('add comment', () =>{
    it('visit', () => {
        cy.visit('http://localhost:3000')
    })

    // it('click', () => {
    //     cy.get('input#username.input').type('admin')
    // })
})